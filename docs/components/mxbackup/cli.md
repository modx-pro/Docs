---
title: CLI и cron
description: Командная строка mxBackup — backup, estimate, cancel, dry-run, проверки, удалённое хранилище, восстановление, параметры запуска, коды возврата и примеры cron.
outline: [2, 3]
lastUpdated: true
---

# CLI и cron

Консольный запуск — основной режим работы mxBackup. Веб-запрос ограничен
таймаутами PHP и обратного прокси, поэтому копию сколько-нибудь крупного сайта
надо снимать из консоли или по расписанию.

```bash
php core/components/mxbackup/cli/mxbackup.php <команда> [параметры]
```

Скрипт сам поднимает MODX, поэтому путь к нему указывается полностью, а рабочий
каталог значения не имеет.

## Команды

| Команда | Что делает |
| --- | --- |
| `backup` | Создаёт архив по выбранному профилю |
| `dry-run` | Показывает состав будущего архива, ничего не записывая |
| `estimate` | Подсчитывает выбранные файлы, их общий размер и свободное место без создания архива |
| `cancel` | Безопасно останавливает текущий backup; `--id=RUN_ID` выбирает конкретный запуск |
| `validate-config` | Проверяет конфигурацию, окружение, каталог хранения и доступность базы. Та же проверка доступна кнопкой «Проверить конфигурацию» в менеджере |
| `list-profiles` | Печатает доступные профили и их режимы |
| `cleanup` | Применяет правила хранения к каталогу архивов |
| `remote-list` | Показывает архивы профиля в [удалённом хранилище](remote-storage.md) |
| `remote-check` | Проверяет доступ к хранилищу: чтение, а с `--write` — запись и удаление |
| `restore-check` | Проверяет архив и выдаёт код подтверждения |
| `restore` | Восстанавливает файлы и/или базу данных |
| `help` | Краткая справка (также `-h`, `--help`) |

`backup`, `dry-run` и `restore` печатают результат в JSON — его удобно разбирать
в скриптах мониторинга.

## Параметры

| Параметр | Назначение |
| --- | --- |
| `--profile=NAME` | Профиль запуска; по умолчанию — `mxbackup.default_profile` |
| `--storage-path=/absolute/path` | Каталог архивов для этого запуска |
| `--config=/absolute/path/mxbackup.php` | Общий PHP-файл дополнительных настроек |
| `--format=tar.gz\|zip` | Формат архива |
| `--mail-to=user@example.com` | Включает отправку отчёта на указанный адрес |
| `--no-mail` | Выключает отправку отчёта |
| `--dry-run` | Тот же эффект, что и команда `dry-run` |
| `--id=RUN_ID` | Для `cancel`: остановить указанный выполняющийся запуск |
| `--verbose` | Печатает журнал MODX в консоль |
| `--no-progress` | Выключает индикацию хода работы |
| `--archive=/absolute/path/backup.zip` | Архив для `restore-check` и `restore` |
| `--from-remote=имя-архива.zip` | Взять архив из удалённого хранилища вместо локального пути |
| `--write` | Для `remote-check`: проверить не только чтение, но и запись |
| `--scope=all\|files\|database` | Что именно восстанавливать |
| `--checksum=SHA256` | Ожидаемая контрольная сумма архива |
| `--confirm=TOKEN` | Код подтверждения из `restore-check` |
| `--password-file=/absolute/path/password.txt` | Файл с паролем зашифрованного ZIP |

Параметры, заданные в командной строке, перекрывают всё остальное — полный
порядок разрешения настроек описан в разделе
[Системные настройки](settings).

::: warning Пароль архива в аргументах не принимается
Аргументы командной строки видны в списке процессов и попадают в историю
командной оболочки. Пароль передаётся либо файлом `--password-file`, либо
переменной окружения `MXBACKUP_ARCHIVE_PASSWORD`. Если пароль не передан, а
терминал интерактивный, `restore-check` и `restore` запросят его с отключённым
эхом ввода.
:::

## Коды возврата

| Код | Значение |
| --- | --- |
| `0` | Успешно |
| `1` | Ошибка выполнения: проверка конфигурации не пройдена, копия не создана, восстановление прервано |
| `2` | Неизвестная команда, либо `restore` вызван без `--confirm` (предварительная проверка при этом пройдена, код подтверждения напечатан) |

Для мониторинга этого достаточно: любой код, отличный от нуля, означает, что
копии за этот запуск нет.

## Ход выполнения

::: tip Появилось в 1.7.0-rc / 2.4.0-rc
:::

`backup`, `dry-run` и `restore` показывают, на какой стадии находится запуск.
В терминале строка перерисовывается на месте:

```text
[3/5] Дамп базы [========............] 34/78  43%  modx_site_content
```

Стадия закрывается строкой с итогом и длительностью, и она остаётся в выводе:

```text
[1/5] Сбор файлов — 12 483, 4.1 с
[2/5] Подготовка базы — 78, 0.3 с
[3/5] Дамп базы — 78/78, 51.7 с
```

Стадии backup: сбор файлов, подготовка базы, дамп базы, упаковка архива,
контрольная сумма и — если профиль настроен на
[удалённое хранилище](remote-storage.md) — выгрузка в хранилище. У `dry-run`
стадий две: сбор файлов и подготовка базы. Стадии `restore`: распаковка архива,
страховочная копия, восстановление файлов, импорт базы; последние две зависят от
`--scope`.

Страховочная копия внутри `restore` — это полноценный backup, и свои стадии он
печатает отдельной лентой с отступом:

```text
[2/4] Страховочная копия…
    [1/5] Сбор файлов — 12 483, 4.0 с
    [2/5] Подготовка базы — 78, 0.3 с
```

### Индикация и вывод скриптов

Индикация идёт в **STDERR**. В STDOUT остаётся только JSON-отчёт, поэтому
конвейеры и парсеры работают как прежде:

```bash
php core/components/mxbackup/cli/mxbackup.php backup --profile=prod > report.json
```

В `report.json` попадёт отчёт, индикация останется на экране. Чтобы убрать и её,
отправьте STDERR в `/dev/null` или передайте `--no-progress`.

### В cron и в логе

Когда вывод перенаправлен в файл или уходит письмом cron, строка не
перерисовывается: печатается только начало стадии и её итог с длительностью.

```text
[3/5] Дамп базы…
[3/5] Дамп базы — 78/78, 51.7 с
[4/5] Упаковка архива…
```

По такому логу видно, на чём именно оборвался неудачный прогон — у последней
стадии не будет строки с итогом.

## Безопасная остановка

Команда запускается из второго терминала или отдельного задания:

```bash
php core/components/mxbackup/cli/mxbackup.php cancel
```

Она переводит активную запись истории в «Останавливается» и создаёт служебный
сигнал рядом с lock-файлом. Backup проверяет его при обходе файлов, дампе БД,
упаковке, подсчёте checksum и выгрузке в S3. На ближайшей безопасной точке SQL
snapshot откатывается, временный архив и дамп удаляются, lock освобождается, а
итоговый статус становится «Остановлен».

Отмена кооперативная: единичный зависший запрос MySQL или системно зависшая
операция диска завершится только после возврата управления в mxBackup. Удалять
`.mxbackup.lock` вручную нельзя — это не остановит процесс и разрешит опасный
параллельный запуск.

## Примеры

Ежедневная production-копия в 03:15, без письма:

```text
15 3 * * * /usr/bin/php /path/to/site/core/components/mxbackup/cli/mxbackup.php backup --profile=prod --no-mail
```

Еженедельная обезличенная копия в отдельный каталог:

```text
30 4 * * 0 /usr/bin/php /path/to/site/core/components/mxbackup/cli/mxbackup.php backup --profile=dev --storage-path=/srv/backups/dev
```

Копия с отчётом на почту и записью вывода в файл:

```text
0 2 * * * /usr/bin/php /path/to/site/core/components/mxbackup/cli/mxbackup.php backup --profile=prod --mail-to=admin@example.com >> /var/log/mxbackup.log 2>&1
```

Разовая копия перед обновлением сайта:

```bash
php core/components/mxbackup/cli/mxbackup.php backup --profile=prod --format=zip
```

### Рекомендации по расписанию

- **Запускайте от пользователя сайта.** Копия, снятая под `root`, оставит
  архивы, которые веб-пользователь не сможет ни прочитать, ни удалить при
  ротации.
- **Не ставьте два задания на одно и то же время.** Второй запуск упрётся в
  блокировку каталога и завершится ошибкой — это защита от порчи архива, а не
  очередь.
- **Отдельная задача `cleanup` не нужна:** ротация выполняется в конце каждого
  успешного запуска. Команда пригодится, если правила хранения изменились и
  применить их надо немедленно. Свежие `mxbackup.retention_count` архивов
  ротация не удаляет ни при каком сроке хранения — см.
  [Системные настройки](settings).
- **`--verbose` — для разбора проблем**, а не для расписания: в cron он
  превращает вывод в поток журнала MODX.

## Диагностика

**`Другой backup уже выполняется`** — в каталоге архивов удерживается блокировка
`.mxbackup.lock`. Это либо реально идущая копия, либо процесс, снятый по
таймауту. Файл блокировки намеренно не удаляется после завершения; смотреть надо
на записанные в него `pid` и время старта.

**`Каталог хранения внутри webroot запрещён`** — путь ведёт в публичную часть
сайта. Выберите каталог вне webroot; см.
[Права и безопасность](permissions).

**`Отсутствует ext-zip` / `Отсутствует zlib` / `Отсутствует Phar`** — на сервере
нет расширения для выбранной операции. `tar.gz` создаётся через `zlib`, а для
его восстановления требуется `Phar`.

**Копия завершилась со статусом «С предупреждениями»** — архив создан, но
что-то не попало внутрь: символическая ссылка, нечитаемый файл или таблица без
поддержки согласованного снимка. Список — в отчёте запуска и в истории
менеджера.

**Ничего не происходит в cron, а вручную работает** — почти всегда разный PHP.
Укажите в задании тот же исполняемый файл PHP, что выдаёт `which php` под
пользователем сайта, и проверьте, что у него есть нужные расширения.
