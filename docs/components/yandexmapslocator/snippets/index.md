---
title: Сниппеты
description: Обзор сниппетов YandexMapsLocator
---

# Сниппеты

| Сниппет | Пакет | Назначение |
|---------|-------|------------|
| [YandexMapsLocator](YandexMapsLocator) | Free | Список + карта + поиск |

**Pro собственных сниппетов не добавляет.** Фильтры `working_now` и `minishop_product`, поля `is_open_now` и REST подключаются плагином поверх того же сниппета.

Вызов на фронте: **некэшированный** (`[[!...]]`, `{'!...' | snippet}`).
