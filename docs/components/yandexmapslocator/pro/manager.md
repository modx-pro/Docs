---
title: CSV в менеджере
description: Импорт и экспорт точек YandexMapsLocator Pro
---

# CSV в менеджере

После установки Pro откройте **Компоненты → YandexMapsLocator Pro**.

Нужно право `save_document`.

## Что умеет

- экспорт точек контейнера в CSV
- импорт CSV (создание и обновление ресурсов)

## Поля формы

| Поле | Описание |
|------|----------|
| `parent` | ID контейнера (обязательно) |
| `template` | Шаблон новых ресурсов (опционально, import) |
| CSV | Текст или файл |

## Колонки CSV

`id`, `pagetitle`, `address`, `latitude`, `longitude`, `phone`, `email`, `category`, `working_hours`.

Файл экспорта: `yandexmapslocator-locations.csv`, UTF-8 BOM.

### Пример строк

```csv
id,pagetitle,address,latitude,longitude,phone,email,category,working_hours
15,"Магазин на Ленина","Омск, ул. Ленина, 25",54.9893,73.3682,"+7 3812 00-00-00",shop@example.com,аптека,"{""mon"":[""09:00-21:00""],""tue"":[""09:00-21:00""],""wed"":[""09:00-21:00""],""thu"":[""09:00-21:00""],""fri"":[""09:00-22:00""],""sat"":[""10:00-22:00""],""sun"":[""10:00-20:00""]}"
16,"Пункт выдачи","Омск, пр. Мира, 10",54.9921,73.3710,"+7 3812 11-11-11",,склад,
```

- Пустой `id` при импорте: создаётся новый ресурс под `parent`.
- Заполненный `id`: обновляется существующий (если найден).
- `working_hours`: JSON в одной ячейке (экранируйте кавычки как в CSV) или оставьте пустым / текст для отображения без «открыто сейчас».

## Контекст экспорта

Экспорт ищет точки в `yandexmapslocator_default_context` (по умолчанию `web`), **не** в контексте менеджера. Точки в другом контексте: смените настройку или перенесите ресурсы.

## Типичный сценарий

1. Экспортируйте контейнер `parent=42`.
2. Поправьте координаты и телефоны в таблице.
3. Импортируйте тот же CSV с тем же `parent` (и `template` для новых строк без `id`).
4. Проверьте публикацию и сниппет на сайте.
