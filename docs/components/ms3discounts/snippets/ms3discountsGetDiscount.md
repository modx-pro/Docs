---
title: Сниппет ms3discountsGetDiscount
description: Бейдж скидки на карточке товара и подготовка строк для msProducts
---

# Сниппет ms3discountsGetDiscount

<!-- MEDIA: screenshot-front | must | Бейдж скидки на карточке товара с ценами до и после скидки | Открыть товар со скидкой на витрине -->
<!-- ![Бейдж скидки на карточке товара](/components/ms3discounts/screenshots/storefront-badge.png) -->

Сниппет рассчитывает лучшую доступную скидку для одного товара. Работает в двух режимах.

```mermaid
flowchart TD
  Input["Вызов сниппета"] --> Check{"Есть id и pagetitle в свойствах?"}
  Check -->|Да (prepareSnippet)| Prepare["Отбор по show_in_catalog<br>Дописывание полей в строку товара<br>Возврат массива для msProducts"]
  Check -->|Нет (карточка товара)| Card["Отбор по show_in_product<br>Подключение CSS и JS<br>Вывод чанка tpl"]
```

**prepareSnippet для msProducts**: если во входящих параметрах одновременно присутствуют поля `id` и `pagetitle`, сниппет определяет строку каталога. Он рассчитывает скидку по акциям с включённым флагом `show_in_catalog`, дописывает поля в массив товара и возвращает его без вызова чанка.

**Карточка товара**: если поле `pagetitle` отсутствует, сниппет определяет вызов на карточке. Он берет `id` текущего ресурса, отбирает акции с включённым флагом `show_in_product`, подключает CSS/JS и выводит чанк.

Если товару подходит сразу несколько акций, сниппет выбирает правило с наибольшей суммой скидки, а при равенстве сумм — с наибольшим процентом. Расчёт учитывает группы авторизованного пользователя и скидки, активированные в текущей сессии через `ms3discounts_activator`.

## Параметры

| Параметр | По умолчанию | Описание |
| --- | --- | --- |
| `id` | ID текущего ресурса | ID товара MiniShop3. Доступен алиас `product` |
| `sale` | пусто | Список ID акций через запятую. Если не указан, сниппет проверяет плейсхолдер `ms3discounts.sale` (его автоматически выставляет `ms3discountsBuyNow`). Если плейсхолдер пуст, проверяются все активные акции |
| `tpl` | `tpl.ms3discounts.get` | Имя чанка для отображения бейджа на карточке |
| `return` | `tpl` на карточке, массив в prepare | Формат возврата: `tpl` (разметка чанка), `data` (массив данных), `json` (строка JSON). В режиме prepare значение `tpl` игнорируется |
| `frontend_css` | пусто | Путь к CSS. Пустая строка использует системную настройку `ms3discounts_frontend_css`. Значение `0` отключает регистрацию стилей |
| `frontend_js` | пусто | Путь к JS. Пустая строка использует настройку `ms3discounts_frontend_js`. Значение `0` отключает регистрацию скрипта |

Для точного расчёта без обращения к базе данных можно передать параметры товара вручную: `price`, `category_ids`, `vendor_id`, `article`, `count`, `options`. Если они не переданы, сниппет запрашивает снимок товара из каталога MiniShop3.

## Плейсхолдеры и поля строки

В режиме карточки эти поля передаются в чанк. В режиме `prepareSnippet` они дописываются в массив товара.

| Поле | Смысл |
| --- | --- |
| `sale_discount` | Процент скидки (если больше 0), иначе сумма скидки |
| `remains` | Количество секунд до даты окончания акции `date_end` |
| `price` | Итоговая цена товара с учётом скидки |
| `base_price` | Исходная цена товара до применения скидки |
| `discount` | Сумма скидки в валюте магазина |
| `percent` | Процент скидки |
| `name` | Название применённого правила скидки |
| `id` | В режиме карточки — ID применённой акции. В режиме prepare сохраняется исходный ID товара |
| `sale_id` | ID применённой акции в режиме prepareSnippet |
| `date_end` | Дата окончания акции в формате `YYYY-MM-DD HH:mm:ss` |
| `potential` | Флаг наличия условий корзины, которые не могут быть проверены на витрине |
| `idx` | Порядковый номер строки начиная с 0 (при выводе через чанк) |

## Примеры вызова

Вывод бейджа на карточке товара:

::: code-group

```fenom
{'!ms3discountsGetDiscount' | snippet : [
  'id' => $_modx->resource.id,
  'tpl' => 'tpl.ms3discounts.get',
]}
```

```modx
[[!ms3discountsGetDiscount?
  &id=`[[*id]]`
  &tpl=`tpl.ms3discounts.get`
]]
```

:::

Вывод каталога товаров с автоматическим расчётом скидок:

::: code-group

```fenom
{'!msProducts' | snippet : [
  'parents' => 0,
  'prepareSnippet' => 'ms3discountsGetDiscount',
  'tpl' => 'tpl.ms3discounts.buynow.row',
]}
```

```modx
[[!msProducts?
  &parents=`0`
  &prepareSnippet=`ms3discountsGetDiscount`
  &tpl=`tpl.ms3discounts.buynow.row`
]]
```

:::
