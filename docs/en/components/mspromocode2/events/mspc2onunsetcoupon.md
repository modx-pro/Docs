# mspc2OnUnsetCoupon

Fires after the promo code is removed from the cart or order.

## Parameters

- `array $coupon` — promo code array
- `null|msOrder $order` — `msOrder` object or `null` if removed from cart
