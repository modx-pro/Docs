# mspc2OnBeforeSetCoupon

Fires before applying the promo code to the cart or order.

## Parameters

- `array $coupon` — promo code array
- `null|msOrder $order` — `msOrder` object or `null` if applying to cart
